
Implement the full Gaussian mixture model given in lectures (The EM
algorithm for Gaussian mixtures). Each Gaussian now has a full covariance
matrix: where each covariance matrix now has four parameters that must
be learned from the data.